const User = require('../../../models/User');

const getUser = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

const getUserById = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    getUser,
    getUserById
};