const Brand = require('../../../models/Brand');

const getBrand = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

const getBrandById = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    getBrand,
    getBrandById
};