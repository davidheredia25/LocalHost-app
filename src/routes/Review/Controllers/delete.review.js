const Review = require('../../../models/Review');

const deleteReview = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    deleteReview
};