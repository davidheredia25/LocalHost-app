const Review = require('../../../models/Review');

const createReview = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    createReview
};