const Types = require('../../../models/Ticket');

const updateTypes = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    updateTypes
};