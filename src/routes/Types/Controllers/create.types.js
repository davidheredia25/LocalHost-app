const Types = require('../../../models/Ticket');

const createTypes = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    createTypes
};