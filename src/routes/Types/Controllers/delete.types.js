const Types = require('../../../models/Ticket');

const deleteTypes = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    deleteTypes
};