const Types = require('../../../models/Ticket');

const getTypes = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

const getTypesById = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    getTypes,
    getTypesById
};