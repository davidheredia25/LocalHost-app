const Ticket = require('../../../models/Ticket');

const deleteTicket = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    deleteTicket
};