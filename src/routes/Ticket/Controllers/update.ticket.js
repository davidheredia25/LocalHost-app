const Ticket = require('../../../models/Ticket');

const updateTicket = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    updateTicket
};