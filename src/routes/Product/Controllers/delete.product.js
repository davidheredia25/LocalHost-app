const Product = require('../../../models/Product');

const deleteProduct = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    deleteProduct
};