const Product = require('../../../models/Product');

const createProduct = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    createProduct
};