/* const Product = require('../../../models/Product');
const { 
    verificacionName,
    
} = require('./middleware');

const createProduct = async (req, res) => {
    const { 
        name,
        brand,
        categories, 
        types, 
        price, 
        color, 
        talle, 
        stock
    } = req.body;
    try {
        let verificacion = 
    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    createProduct
}; */