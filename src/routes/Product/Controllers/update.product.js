const Product = require('../../../models/Product');

const updateProduct = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    updateProduct
};