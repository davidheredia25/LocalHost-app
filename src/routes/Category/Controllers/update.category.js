const Category = require('../../../models/Category');

const updateCategory = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    updateCategory
}