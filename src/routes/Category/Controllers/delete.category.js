const Category = require('../../../models/Category');

const deleteCategory = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    deleteCategory
}