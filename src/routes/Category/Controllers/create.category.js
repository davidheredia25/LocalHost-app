const Category = require('../../../models/Category');

const createCategory = async (req, res) => {
    try {

    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    createCategory
}