const Category = require('../../../models/Category');

const createCategory = async (req, res) => {
    const { name } = req.body;
    try {
        // let verificacion = 
    } catch (error) {
        console.log(error);
    }
};

module.exports = {
    createCategory
}